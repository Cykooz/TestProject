[buildout]
extends = buildout.cfg
extensions += buildout.dumppickedversions
allow-picked-versions = true

[django]
arguments = 'testproject.development'

[wsgi]
config-file = ${buildout:directory}/etc/debug.ini